

First you tick the file in the git section

Then commit it and add a comment

Then change it.

After you have changed it you then tick it look at the differents, tick the button in the open window and
then commit it

Press push to upload to github